const {
  maps,
  filters,
  findIndexes,
  reduces,
  everys
} = require('./index');

describe('tests functions similar to array operations', () => {
  it('', () => {
    

    expect().toEqual();
  });

});
